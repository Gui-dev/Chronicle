pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: pnpm biome check --no-errors-on-unmatched --staged {staged_files}
      stage_fixed: true

pre-push:
  commands:
    test:
      run: pnpm test
